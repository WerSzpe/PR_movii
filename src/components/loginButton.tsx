import React from 'react';

const LoginButton = (props : any) => {
    return(
        <button type="button" className="btn btn-dark btnLogin">Logowanie/rejestracja</button>
    );
}

export default LoginButton;